# 场景一术语表

## 本地联动Agent POC
在本地浏览器和本地Node服务之间形成可操作闭环的验证体。驾驶舱负责展示和交互，本地服务负责调用 `local-agent/agent.mjs` 读取样例数据、运行规则判断与GLM研判模拟，并返回结构化结果。

## 受控型质量Agent
面向混凝土生产质量判断的Agent方案形态。它可以完成输入结构化、规则判断、质量研判、异常分支和台账输出，但出料、补水、暂停等高风险动作必须保留人工确认或授权。

## HITL人工确认
Human-in-the-loop。用于表达质检员或操作员在异常处置、调整授权、放行确认中的责任边界。它不是临时补丁，而是工业质量场景的安全边界。

## 样例数据
用于本地POC验证的结构化批次输入，包括视觉特征、电流时序、配比、气象、运距和设备状态。样例数据不等同于真实产线实时数据流。

## 真实产线接入
指后续接入真实PLC、ERP、CQMS、视频流和生产控制接口。当前本地联动Agent POC尚未完成真实产线接入。
